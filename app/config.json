{
    "integrations": [
        {
            "name": "example",
            "destination": "http://backend.example.com",
            "in_rate_limit": 100,
            "out_rate_limit": 1000,
            "incoming_auth": [
                {"type": "token", "params": {"token": "secret", "header": "X-Auth"}}
            ],
            "outgoing_auth": [
                {"type": "token", "params": {"token": "secret", "header": "X-Auth"}}
            ]
        }
    ]
}
