- integration: slack
  callers:
    - id: "*"
      rules:
        - path: /api/chat.postMessage
          methods:
            POST:
              body:
                type: object
                properties:
                  channel:
                    type: string
                    const: bad-channel
                required:
                  - channel
